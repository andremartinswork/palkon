import React from 'react'
import Section from '../../atoms/section'

export default function Numbers(props) {
  const {
    id,
    title,
    items
  } = props;

  return (
    <Section id={id}>
      Numbers
    </Section>
  )
}
