import React from 'react';

// ATOMS
import Section from '../../atoms/section';

export default function WhyUs(props) {
  const {
    id,
    image,
    smallTitle,
    items
  } = props;

  return (
    <Section id={id}>
      
    </Section>
  )
}
