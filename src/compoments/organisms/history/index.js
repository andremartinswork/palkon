import React from "react";

// ATOMS
import Section from "../../atoms/section";

export default function History(props) {
  const { id, smallTitle, text } = props;

  return <Section id={id}>History</Section>;
}
